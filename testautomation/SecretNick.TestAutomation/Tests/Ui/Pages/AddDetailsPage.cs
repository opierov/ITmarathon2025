using Microsoft.Playwright;

namespace Tests.Ui.Pages
{
    public class AddDetailsPage(IPage page) : BasePage(page)
    {
        // This page inherits all functionality from BasePage
    }
}
